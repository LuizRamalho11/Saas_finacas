# Banco de dados — troque livremente por Neon, Supabase ou um Postgres próprio.
DATABASE_URL="postgresql://finora:finora@localhost:55432/finora?schema=public"

# Segredo de assinatura dos tokens de sessão (gere o seu com: openssl rand -base64 32)
AUTH_SECRET="gere-com-openssl-rand-base64-32"
AUTH_TRUST_HOST=true
